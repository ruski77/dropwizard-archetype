#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )

#!/bin/sh

cd $OPENSHIFT_REPO_DIR/diy

mvn -s $OPENSHIFT_REPO_DIR/.openshift/settings.xml package -DskipTests=true